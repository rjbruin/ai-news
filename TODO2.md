Do all of the below. Keep iterating until done. Do not ask for permission to continue, you have it. Just keep going, even if it's a lot. Group changes in PRs. Once done, merge all PRs and publish a new minor release.

* Replace the newspaper emoji on the non-authenticated frontpage with the app logo.
* Update the descriptions on the non-auth frontpage to be more in-line with current functionality.
* Add the most recent admin-created link-shared written edition (without PDF and podcast links) to the non-auth frontpage as a demo. Put a box to it, like on the auth homepage, on the frontpage. Put the box with the list of sources also on the non-auth frontpage.
* Restrict account registration: add a feature for admins to create and manage invite links with a configured number of uses; also add the ability for admins to turn on or off inviteless registration (off for now). When inviteless registration is off, make the register page display a message explaining an invite is needed to register.
* Check that the magic link sign-in works. I think I've had issues with it.
* Check that anything publicly accessible, without an account, is watertight against intrusion. 
* Tweak fonts and other styling things (at your discretion) to make the app look less Bootstrap-y and a bit more modern.
* Allow users to turn on or off sources *only for their own editions*. Make this configurable by toggle in the Sources page. By default, turn on all sources.
* Hide costs of admin-paid sources on the Sources page. Only show this on the Admin page. Change the column from "API Key" to "Payment". Make it say "Included in system" for the global API key; "your API key" for the user's key; "another user's API key" for another users key. Do not show Usage for the global or other users' keys, only for the own keys: remove the Usage column and put the usage cost in the Payment column on a newline below the "your API key" line. Show the usage cost as "$x.xx total, $x.xx in the last week". Add the required tracking for such historical data. Do not put token usage.
* Rename the API Keys page the Payment page. Add to the top of the page a hero item "What are API keys?". Give it a single-line description ("To cover the cost of the AI models that make this app work, you need to create an account with the website OpenRouter, and provide this app with an [i]API key[/i].), and a button ("More about API keys") to open a modal with a full explanation, including instructions for how to make one on OpenRouter. Assume the user is unfamiliar with these concepts. This layout should help them setup their own payment for their app usage. Include in the Add a key section a short description of the expected costs of making an edition and of polling a news source.
* Create an onboarding tutorial, triggered when a user finishes registration and first visits the homepage. Run through the basic functionality of the system, highlighting configurability of the features. Make sure it explains that the system runs on AI models that cost money, and that they need to set up an API key on the corresponding page.